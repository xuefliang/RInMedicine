---
title: "前言"
author: "梁雪枫"
date: "2014年10月11日"
documentclass: ctexart
output:
  pdf_document:
    fig_caption: yes
    latex_engine: xelatex
    number_sections: yes
    template: !expr rticles::ctex_template()
    toc: yes
classoption: "hyperref`r if (.Platform$OS.type != 'windows') ',nofonts'`"
---
R目前仍然不为大众所了解，但并不能妨碍其流行趋势，越来越多的人和行业开始使用R做为数据统计工具。相对于R在其他行业的流行，医学领域R应用更加少见，医学领域目前仍然以SPSS和SAS为主，本书主要目的在于将R应用引入到医学领域。

本书使用 markdown衍生版本R Markdown（Rmd）V2进行撰写，Markdown是一种非常简洁的标记语言，足以使写作关注于内容而不是形式。本书是在Linux Mint系统上安装R的，需要在shell中安装如下的库文件

```{r eval=FALSE}
sudo apt-get install build-essential gfortran libxml2-dev libcurl4-openssl-dev libfreetype6-dev libbz2-dev liblapack-dev  libpcre++-dev liblzma-dev r-cran-rcpp r-cran-rjava openjdk-7-* libgmp3-dev libmysql++-dev libmpfr-dev

#Step 1: Update Sources.List File
#- Edit the sources.list file
sudo gedit /etc/apt/sources.list
#- Add following entry
deb http://cran.rstudio.com/bin/linux/ubuntu utopic/
#Step 2: Add the Public Keys
gpg --keyserver keyserver.ubuntu.com --recv-key E084DAB9
gpg -a --export E084DAB9 | sudo apt-key add -
#Step 3: Install R-base
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install r-base r-base-dev
```
安装Java的jdk时，在shell中运行 sudo R CMD javareconf命令，解决jdk安装问题。

升级
```{r eval=FALSE}
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install r-base
cp -r ~/R/x86_64-pc-linux-gnu-library/3.1/* ~/R/x86_64-pc-linux-gnu-library/3.2 # at the shell prompt
update.packages(checkBuilt=TRUE, ask=FALSE) #at the R prompt 
```


本书涉及的R语言包较多，CRAN中包可通过下列方式一次性完成安装。
```{r eval=FALSE}
wants <- c("knitr","rmarkdown","devtools","epicalc","mosaic","showtext","pander","PerformanceAnalytics","fitdistrplus","CircStats","MASS","mixtools","boot","TrialSize","vcd","ggplot2","pspearman","gvlma","car","lmtest","leaps","plyr","bootstrap","elrm","rms","Deducer","bestglm","survival","robust","mlogit", "nnet","VGAM","ordinal","Sample.Size","phia","mvtnorm","pscl","mosaic","XML","pipeR","Rcmdr","rgl","HH","DescTools","multcomp","effects","sandwich","qcc")

has   <- wants %in% rownames(installed.packages())

if(any(!has)) install.packages(wants[!has])
```
GitHub中R包通过以下方式进行安装
```{r eval=FALSE}
devtools::install_github("rstudio/rticles")
devtools::install_github("rstudio/rmarkdown")
```
bioconductor中R包通过以下方式进行安装
```{r eval=FALSE}
source("http://bioconductor.org/biocLite.R")
biocLite(c("GenomicFeatures", "AnnotationDbi"))
```
R-Forge等其它第三方源中的包，通过以下方式进行安装
```{r eval=FALSE}
install.packages("blotter", repos="http://R-Forge.R-project.org")
```


样本量计算，由于水平有限对微阵列数据基于族错误率(FWER)样本量计算和基于贝叶斯统计的样本量计算未能实现，留有遗憾。

本书定位于对R有简单了解读者，如果读者对基本的语法和相关内容不熟悉，本书不做详细介绍，还请读者参考其他教程。本书假定读者对R、Rmd、Git等相关知识有初步的了解，源代码和书中使用相关数据文件会公布于GitHub（https://github.com/xuefliang/RInMedicine）， 请读者clone在本地进行学习。本书以实际案例分析为主要内容，细致讲解案例所涉及统计学和R语言的相关内容，解决实际工作中存在的问题，也可以本书Rmd为模板，学习如何使数据分析的过程可重复。如何利用R进行实验设计尚不在本书介绍范围。

由于作者水平有限，时间仓促，书中难免有错误和疏漏之处，诚恳地期望各位专家和读者批评指正，对此，将深表感谢。本书源代码托管于GitHub，期望得到共同修缮，联系方式QQ：360149345。